// what should cointain what?
// should have a card obj that cointains data, is sorted and is pusched into a deck array
// the deck should then be shuffled. 

function Card(s,r) {
    this.suit = s;
    this.rank = r;
}

